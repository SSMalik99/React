//this module will provide a secret for the user on which basis token will generate;
module.exports={
    'secret':'MySecret'//this secret can be any key and value but when we use it then must be same as it is provided here;
}